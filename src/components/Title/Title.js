import React from "react";
import "./Title.css";

const Title = props => (
    <div className="titlebox">
        <h1>Memory Click Game!</h1>
        <p>Click on a picture and earn a point. Click the same picture more than once, you lose and start all over.</p>
    </div>
);

export default Title;